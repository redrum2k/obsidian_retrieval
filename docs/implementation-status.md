# Implementation status

Local implementation exists; the live MVP is not yet released. Huyen is excluded from the initial live scope at the owner's request.

## Verified locally

`uv run pytest -q`: **29 passed**. `uv run ruff check src tests examples` and formatter checks pass. PyMuPDF emits dependency deprecation warnings; they do not fail the tests.

Coverage includes:

- SQLite FTS5 title/alias ranking, source locators, bounded Unicode output and continuations.
- Incremental refresh, same-content rename, deletion, forced hash reconciliation, stale-source rejection, cached extraction across restart, and rebuild without dropping pending proposals.
- Sensitive/code exclusions, CS study-document exceptions, future CS discovery, symlinks/hardlinks, context-only revocation, and cache cleanup after exclusion.
- Markdown, DOCX, PPTX, text PDF, scanned-PDF OCR/rendering, embedded Office attachment manifests, and visible corrupt-extraction failures.
- External schedule calculations across both daylight-saving transitions, Raw-first pending work, duplicate-trigger locking, notification retry/acknowledgment, and unchanged-pending silence.
- Exact signed-approval verification, source/target conflicts, multi-file note creation/update and JSON-log preservation, failure after note creation before log completion, idempotent recovery, and rejection of divergent edits during recovery.
- Derived Raw companions that do not recursively become new intake, and indexed backlinks with rename warnings without automatic repairs.
- The requested end-to-end sequence using a simulated external trigger, isolated source files, a test-only signing key, and simulated host delivery: discovery → concrete proposal → approval → note creation/update → processing log → unchanged quiet run.

The test key/host are fixtures, not a connected live approval system.

## Synthetic efficiency check

[Reproducible report](synthetic-evaluation.json), generated by `examples/evaluate.py`:

- 30 exact-term lookup tasks against one synthetic 79 KB source.
- Approximately 93% fewer tool-output tokens than one full-source read per task.
- All 30 expected sections found; zero successful re-extractions on unchanged reconciliation.

These are engineering smoke results only. They do not measure owner-labeled paraphrases, answer quality, complete agent-task cost, real-corpus p95 latency, or the PRD's repeated-span metric. They do not satisfy M4's release evaluation.

## Milestone state

| Milestone | State |
| --- | --- |
| M0 — configuration and baseline | Live conventions inspected; exact disabled config proposed; owner review and labeled corpus baseline pending |
| M1 — inventory and extraction | Implemented with isolated fixtures; corpus-essential format coverage awaits approved live validation |
| M2 — retrieval | CLI implemented and tested; external heartbeat has not been wired to it |
| M3 — scheduled/approved processing | Coordinator and write/recovery protocol tested; real conversation delivery and trusted approval adapter pending |
| M4 — release | Not complete; no production savings/accuracy claim |

## Remaining release work and limits

1. **Configuration approval:** the owner explicitly requested review before live indexing. [Proposed JSON](../examples/live-config.proposed.json) stays disabled; no live state directory or study-note writes were created.
2. **Approval integration:** the existing Codex conversation remains the intended surface, but no trusted conversation-event-to-signature bridge is available in this project. Do not give the agent a signing key or substitute an `approved: true` flag. The public key is unset in the live draft.
3. **Host integration:** the CLI supports intake and plan submission, but the existing heartbeat has not been modified. Grounded synthesis and user notification belong to that host, not to retrieval. A standalone CLI invocation without a plan returns pending work; it does not invent study notes.
4. **Essential formats:** Tesseract was found locally; LibreOffice was not. Word/slide text extraction and attachment caching work, but Office rendering/legacy conversion and visual fidelity require a local renderer plus corpus validation. No live scans/slides were opened during this implementation.
5. **External editing race:** per-tool locking, staged writes, no-follow access, hash checks and recovery prevent tested conflicts. There is no portable atomic compare-and-swap against unrelated Obsidian/editor processes. Resolve or explicitly accept that remaining final-check/replacement race before live writes; no cross-application transaction guarantee is claimed.
6. **Role ambiguity:** known generated/context-only registry entries are respected. New ungrounded live inputs stay metadata-only pending triage; filename filters cannot establish authorship or prove an ambiguously named document is nonsensitive.
7. **Evaluation:** run the PRD's owner-labeled tasks, including held-out paraphrases and complete agent traces, before accepting recall/token/latency targets. Embeddings remain deferred.
8. **Retention and reports:** pending history/backups are retained indefinitely in private external state. Operational intake state is external; automatic dated Planning proposal/report files and lifecycle attachment moves are not implemented. Those remain with the existing workflow unless an explicit adapter is selected.

`architecture-proposal.md` remains byte-for-byte unchanged (SHA-256 `a3e7f52c1dfce6530f104f9d06147ecb33fee34cb9fe28e330889c9991fd30d0`).
